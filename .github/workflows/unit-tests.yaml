name: Unit tests

on:
    push:
        branches: [ main ]
    pull_request:
        branches: [ main ]

jobs:
    unit-tests:
        runs-on: ubuntu-latest
        steps:
            - name: Run tests
              run: npm run test:ci